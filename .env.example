# Environment variables for local development
# Embr will inject DATABASE_URL at runtime in the cloud
# For local dev, set a valid PostgreSQL connection string:
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"
